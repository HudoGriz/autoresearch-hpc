{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/discovery-loop/schema/claim.schema.json",
  "title": "Iteration claim",
  "description": "Written by `dl claim` when an iteration number is taken. The directory creation is the lock; this file records who holds it.",
  "type": "object",
  "required": ["iteration", "agent", "claimed", "title", "status"],
  "additionalProperties": false,
  "properties": {
    "iteration": { "type": "integer", "minimum": 1 },
    "agent": { "type": "string", "description": "Harness that executed the claim, detected from the environment." },
    "declared_agent": { "type": "string", "description": "Agent name the operator declared, which may differ from the detected one." },
    "claimed": { "type": "string", "format": "date-time" },
    "title": { "type": "string", "minLength": 1, "description": "The question, stated as a question." },
    "status": { "enum": ["CLAIMED", "PREDECLARED", "RUNNING", "CONCLUDED", "ABANDONED"] },
    "session": { "type": "string" }
  }
}
